let addNumbers2 = (numbers: number[]) => {
    let total = 0
    for (let i = 0; i < numbers.length; i++) {
        total += numbers[i]
    }
    return total;
}
let numbers: number[] = [5, 2, 3, 9]
console.log(addNumbers2(numbers))
